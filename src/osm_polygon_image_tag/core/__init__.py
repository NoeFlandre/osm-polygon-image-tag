"""Core contracts: configuration, errors, schema, manifest, and progress.

Modules in this package define the data shapes, error hierarchy, and
protocol-versioning rules that every other layer consumes. Nothing here may
import from the runtime, artifacts, ingest, or integrations subpackages.
"""
