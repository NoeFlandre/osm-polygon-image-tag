# Asset unit tests

These tests lock the deterministic asset schema, manifests, reference
canonicalization, cache, storage, and shard builder contracts. They use only
temporary local files and injected provider boundaries.
